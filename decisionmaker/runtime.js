const scheduler = (config)=>{

}

const checkRequestQueue = (config)=>{
    
}

const checkIntrupt = (config)=>{
    
}

const requestHandler = (config) => {

}

const taskHandler = (config) => {
    
}

const intruptHandler = (config) => {
    
}